#pragma once
#include <string.h>

using namespace std;

class Init {
    public:
        int init(int i, int ie, int oe, string n, int b, int d, int s, int q);
};

struct banNam {
  string nombre;
  int bandeja;
};